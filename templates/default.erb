<%= ERB.new(File.read(File.expand_path("_header.erb",File.dirname(file)))).result(binding) -%>

repo_add_once="<%= @repo_add_once %>"
repo_reenable_on_distupgrade="<%= @repo_reenable_on_distupgrade %>"
